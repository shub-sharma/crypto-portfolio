/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "assets.coingecko.com",
      "img.clerk.com",
      "coin-images.coingecko.com",
    ],
  },
};

export default nextConfig;
